version: '3.8'

services:
  ciphernest:
    build: .
    container_name: ciphernest-bot
    env_file:
      - .env
    restart: unless-stopped
    # Optional: Redis for production state management
    # depends_on:
    #   - redis
    
  # redis:
  #   image: redis:7-alpine
  #   container_name: ciphernest-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

# volumes:
#   redis_data:
